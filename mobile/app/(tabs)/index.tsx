/**
 * Home Screen - Beautiful HeroUI-inspired Interface
 * Features: Animated avatar, gradient design, chat interface
 */

import React from 'react';
import { HomeScreen } from '../../components/HomeScreen';

export default function HomeScreenTab() {
  return <HomeScreen />;
}